<%= form_with(model: contest_category, local: true) do |form| %>
  <% if contest_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contest_category.errors.count, "error") %> prohibited this contest_category from being saved:</h2>

      <ul>
      <% contest_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
