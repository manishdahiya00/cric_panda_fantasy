<%= form_with(model: user_contest, local: true) do |form| %>
  <% if user_contest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_contest.errors.count, "error") %> prohibited this user_contest from being saved:</h2>

      <ul>
      <% user_contest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
